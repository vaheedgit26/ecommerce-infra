apiVersion: v1
kind: ConfigMap
metadata:
  name: user
data:
  ENVIRONMENT: "dev"
  AWS_REGION: "us-east-1"
  DB_HOST: "ecommerce-dev-rds-postgres"
  DB_PORT: "5432"
  DB_NAME: "ecommercedb"
  # DB_USER: "postgres"       # Create as secrets by eso operator
  # DB_PASSWORD: ""           # Create as secrets by eso operator
