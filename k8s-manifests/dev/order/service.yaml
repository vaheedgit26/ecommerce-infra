apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app.kubernetes.io/name: order
    app.kubernetes.io/owner: ecommerce 
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8004
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: order
    app.kubernetes.io/owner: ecommerce
